# Expo inlines any EXPO_PUBLIC_* var into the client bundle at build time —
# nothing secret belongs here, this is just the API's base URL.
# Copy to `.env` and adjust if apps/api's wrangler dev is running elsewhere.
EXPO_PUBLIC_API_URL=http://localhost:8787
